import styled from "@emotion/styled";
import styles from "../../styles/emotion";
import styles () from emotion.styles;

export default function (TipPages) {
  return (
    <Wrapper>
      <Frame>
        <pre>프론트엔드 공부하면서 참고했던 자료들</pre>
      </Frame>
    </Wrapper>
  );
}
